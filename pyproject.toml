[project]
name = "agent-platform"
version = "0.1.0"
description = "Receptionist + outreach agent platform skeleton"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110.0",
  "uvicorn>=0.29.0",
  "pydantic>=2.7.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
